# ============================================================
# requirements.txt
# Web Crawler + RAG Pipeline — Indus Net Technologies Pvt. Ltd.
# ============================================================

# ------------------------------------------------------------
# Web crawling (web_crawler.py)
# ------------------------------------------------------------
aiohttp==3.9.5                  # async HTTP client with connection pooling
aiohttp-retry==2.8.3            # optional: automatic retry on transient errors
beautifulsoup4==4.12.3          # HTML parsing and link extraction
lxml==5.2.2                     # fast parser backend for BeautifulSoup
chardet==5.2.0                  # character-encoding detection for raw bytes

# ------------------------------------------------------------
# Database — MongoDB
# ------------------------------------------------------------
motor==3.4.0                    # async MongoDB driver (wraps pymongo)
pymongo==4.7.2                  # sync MongoDB driver (required by motor)
dnspython==2.6.1                # required for mongodb+srv:// connection strings

# ------------------------------------------------------------
# RAG pipeline core
# ------------------------------------------------------------
langchain==0.2.6                # RAG orchestration framework
langchain-community==0.2.6      # community integrations (loaders, parsers)
langchain-core==0.2.10          # shared primitives used by langchain packages
langchain-groq==0.1.9           # LangChain official wrapper for Groq LLM
groq==0.9.0                     # official Groq Python SDK (async + sync)

# ------------------------------------------------------------
# Embeddings (for RAG vector store — no OpenAI needed)
# ------------------------------------------------------------
sentence-transformers==3.0.1    # local embeddings via HuggingFace (free, no API key)
faiss-cpu==1.8.0                # vector similarity search (swap for chromadb if preferred)

# ------------------------------------------------------------
# Document parsers (used by route_to_parser)
# ------------------------------------------------------------
pypdf==4.2.0                    # PDF text extraction
python-docx==1.1.2              # .docx parsing
openpyxl==3.1.4                 # .xlsx parsing
python-pptx==0.6.23             # .pptx parsing
unstructured==0.14.6            # generic document partitioner (fallback parser)

# ------------------------------------------------------------
# Async / concurrency utilities
# ------------------------------------------------------------
anyio==4.4.0                    # async compatibility layer used by several libs
asyncio-throttle==1.0.2         # optional: per-domain rate limiting helper

# ------------------------------------------------------------
# HTTP server / API layer  (FastAPI stack assumed from src.api)
# ------------------------------------------------------------
fastapi==0.111.0                # async REST API framework
uvicorn[standard]==0.30.1       # ASGI server for FastAPI
pydantic==2.7.4                 # data validation (used by FastAPI)
pydantic-settings==2.3.1        # settings management via .env

# ------------------------------------------------------------
# Logging & observability
# ------------------------------------------------------------
python-json-logger==2.0.7       # structured JSON logging
rich==13.7.1                    # pretty console output during development

# ------------------------------------------------------------
# Utilities
# ------------------------------------------------------------
python-dotenv==1.0.1            # load environment variables from .env
tenacity==8.3.0                 # retry decorator for flaky network calls
httpx==0.27.0                   # sync/async HTTP client (used in tests / utils)